import React from 'react'
import { withLayout } from '@/template'

const Dashboard = () => {
  return (
    <div className='flex flex-1 bg-background justify-center items-center'>Dashboard</div>
  )
}

export default withLayout(Dashboard)